{% if space.reservation_system.name == 'LibCal' %}
  {% capture reserve_url %}{{ space.reservation_system.url }}&gid={{ space.reserve_sys_id }}{% endcapture %}
{% else %}
  {% assign reserve_url = space.reservation_system.url %}
{% endif %}

<a class="ui labeled icon button space-finder__reserve js-spacefinder-link {% if indiv_space %}space__reserve{% endif %}" href="{{ reserve_url }}" title="Reserve {{ space.name }}" target="_blank">
  <span class="calendar icon"></span> Reserve
</a>
